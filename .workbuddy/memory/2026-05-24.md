# 2026-05-24 CoFly 项目日志

## Part 0 - 项目基底与环境
- 完成项目基础配置（project.godot）
- 渲染器：Mobile 模式，Windows 驱动设为 Vulkan，启用 ETC2/ASTC 纹理压缩（移动端适配）
- 视口：854×480，canvas_items 拉伸模式 + keep 宽高比
- 输入映射：throttle_up/down, pitch_up/down, roll_left/right, yaw_left/right（events 待后续绑定触摸/陀螺仪）
- Git 初始化 + 初始提交（commit d53ba60）
- GitHub 仓库未创建（本地无 gh CLI）
- Godot 版本：4.6.2，物理引擎：Jolt Physics
- 模型资源：MD-11 三发客机（GLB + 3 张 PBR 贴图：basecolor/normal/RM）
- 纹理导入递归错误（reimport_files() recursively）：删除 .godot/ 缓存后重新打开编辑器解决

## Part 2 - 真实飞行物理
- 创建 `scripts/simple_flight.gd`（RigidBody3D 扩展）：推力/阻力/升力/重力/力矩计算，`_integrate_forces` 实现
- 创建 `scripts/input_handler.gd`（Node 扩展）：W/S 油门、↑/↓ 俯仰、←/→ 滚转、A/D 偏航的键盘映射，程序化绑定到输入动作
- 创建 `scenes/test_flight.tscn`：完整测试场景（天空+太阳+地面+MD-11 飞机）
- 更新 `project.godot`：设置主场景、补回 aspect=keep
- 已禁用 RigidBody3D 默认重力（gravity_scale=0），由自定义物理接管

## MD-11 模型接入飞控
- 调优物理参数适配 MD-11 大型客机：mass=500, thrust=4000, 升阻系数调低
- 添加重心偏移支持（center_of_mass_offset），MD-11 三发布局重心靠前靠下
- 碰撞盒尺寸从 30×2×25 调整到 26×5.5×22，位置抬高以匹配模型高度
- MD-11 模型子节点添加 180° Y 旋转（GLB 默认 +Z 朝向 → Godot -Z 前进方向）
- 添加追尾摄像机（Camera3D），位置 (0, 5, 18)，面向飞机
- 补回 project.godot 中 rendering_device/driver.windows="vulkan"

## 修复 + Demo 主场景
- 修复 simple_flight.gd center_of_mass 报错：需先设 center_of_mass_mode = CENTER_OF_MASS_MODE_CUSTOM
- 创建 demo.tscn（完整 Demo 场景：天空+地面+MD-11+摄像机+输入），设为主场景
- 更新 project.godot 主场景为 demo.tscn

## 黑屏/MD-11 透明修复
- 创建 `scripts/tail_camera.gd`：用 make_current() 脚本确保摄像头激活（tscn 中 current=true 不可靠）
- 在 demo.tscn 和 test_flight.tscn 的 Camera3D 节点上引用 tail_camera.gd，移除 current=true
- 修复 Environment 环境光设置：ambient_light_source=1（禁用）→ 2（从天空采样），防止暗面全黑
- 彻底解决黑屏根因：Godot 编辑器打开 demo.tscn 时意外移除了 MD-11 实例引用
  - 将 MD-11 模型加载从 tscn 实例化改为 `simple_flight.gd` 代码加载（preload + instantiate）
  - 移除 rendering_device/driver.windows 强制 Vulkan 设置，让 Godot 自动选择最佳驱动
  - 删除 tail_camera.gd.uid（编辑器生成的无效 UID）
